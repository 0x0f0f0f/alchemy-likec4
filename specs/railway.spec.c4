// GENERATED — DO NOT EDIT.
//
// 22 Railway resources, reflected from alchemy@2.0.0-beta.77.
// Regenerate with:  bun run generate
//
// Every kind below is a resource alchemy can provision, identified by the `.Type` it
// registers itself under. Unused kinds are harmless — LikeC4 validates a specification
// with kinds nothing instantiates, so one shared file serves every repo.
//
// Factories and bindings are NOT here: `DurableObject`, `Email.SendEmail` and
// `Website.Astro` are plain functions with no `.Type`. An Astro site IS a worker once
// deployed; bindings appear below as relationship kinds.

specification {
  // ── (root) ──
  deploymentNode railway_bucket {
    notation 'Railway.Bucket'
    style {
      shape rectangle
    }
  }
  deploymentNode railway_cloud_agent {
    notation 'Railway.CloudAgent'
    style {
      shape rectangle
    }
  }
  deploymentNode railway_custom_domain {
    notation 'Railway.CustomDomain'
    style {
      shape rectangle
    }
  }
  deploymentNode railway_environment {
    notation 'Railway.Environment'
    style {
      shape rectangle
    }
  }
  deploymentNode railway_function {
    notation 'Railway.Function'
    style {
      shape rectangle
    }
  }
  deploymentNode railway_group {
    notation 'Railway.Group'
    style {
      shape rectangle
    }
  }
  deploymentNode railway_mongo {
    notation 'Railway.Mongo'
    style {
      shape rectangle
    }
  }
  deploymentNode railway_my_sql {
    notation 'Railway.MySQL'
    style {
      shape rectangle
    }
  }
  deploymentNode railway_postgres {
    notation 'Railway.Postgres'
    style {
      shape rectangle
    }
  }
  deploymentNode railway_private_network {
    notation 'Railway.PrivateNetwork'
    style {
      shape rectangle
    }
  }
  deploymentNode railway_private_network_endpoint {
    notation 'Railway.PrivateNetworkEndpoint'
    style {
      shape rectangle
    }
  }
  deploymentNode railway_project {
    notation 'Railway.Project'
    style {
      shape rectangle
    }
  }
  deploymentNode railway_redis {
    notation 'Railway.Redis'
    style {
      shape rectangle
    }
  }
  deploymentNode railway_sandbox {
    notation 'Railway.Sandbox'
    style {
      shape rectangle
    }
  }
  deploymentNode railway_service {
    notation 'Railway.Service'
    style {
      shape rectangle
    }
  }
  deploymentNode railway_tcp_proxy {
    notation 'Railway.TcpProxy'
    style {
      shape rectangle
    }
  }
  deploymentNode railway_template {
    notation 'Railway.Template'
    style {
      shape rectangle
    }
  }
  deploymentNode railway_usage_limit {
    notation 'Railway.UsageLimit'
    style {
      shape rectangle
    }
  }
  deploymentNode railway_variable {
    notation 'Railway.Variable'
    style {
      shape rectangle
    }
  }
  deploymentNode railway_volume {
    notation 'Railway.Volume'
    style {
      shape rectangle
    }
  }
  deploymentNode railway_volume_backup {
    notation 'Railway.VolumeBackup'
    style {
      shape rectangle
    }
  }
  // ── Website ──
  deploymentNode railway_website_cdn {
    notation 'Railway.Website.Cdn'
    style {
      shape rectangle
    }
  }
}
