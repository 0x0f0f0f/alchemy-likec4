// GENERATED — DO NOT EDIT.
//
// 8 Axiom resources, reflected from alchemy@2.0.0-beta.77.
// Regenerate with:  bun run generate
//
// Every kind below is a resource alchemy can provision, identified by the `.Type` it
// registers itself under. Unused kinds are harmless — LikeC4 validates a specification
// with kinds nothing instantiates, so one shared file serves every repo.
//
// Factories and bindings are NOT here: `DurableObject`, `Email.SendEmail` and
// `Website.Astro` are plain functions with no `.Type`. An Astro site IS a worker once
// deployed; bindings appear below as relationship kinds.

specification {
  // ── (root) ──
  deploymentNode axiom_annotation {
    notation 'Axiom.Annotation'
    style {
      shape rectangle
    }
  }
  deploymentNode axiom_api_token {
    notation 'Axiom.ApiToken'
    style {
      shape rectangle
    }
  }
  deploymentNode axiom_dashboard {
    notation 'Axiom.Dashboard'
    style {
      shape rectangle
    }
  }
  deploymentNode axiom_dataset {
    notation 'Axiom.Dataset'
    style {
      shape rectangle
    }
  }
  deploymentNode axiom_monitor {
    notation 'Axiom.Monitor'
    style {
      shape rectangle
    }
  }
  deploymentNode axiom_notifier {
    notation 'Axiom.Notifier'
    style {
      shape rectangle
    }
  }
  deploymentNode axiom_view {
    notation 'Axiom.View'
    style {
      shape rectangle
    }
  }
  deploymentNode axiom_virtual_field {
    notation 'Axiom.VirtualField'
    style {
      shape rectangle
    }
  }
}
