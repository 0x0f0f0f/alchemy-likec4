// GENERATED — DO NOT EDIT.
//
// 7 Planetscale resources, reflected from alchemy@2.0.0-beta.77.
// Regenerate with:  bun run generate
//
// Every kind below is a resource alchemy can provision, identified by the `.Type` it
// registers itself under. Unused kinds are harmless — LikeC4 validates a specification
// with kinds nothing instantiates, so one shared file serves every repo.
//
// Factories and bindings are NOT here: `DurableObject`, `Email.SendEmail` and
// `Website.Astro` are plain functions with no `.Type`. An Astro site IS a worker once
// deployed; bindings appear below as relationship kinds.

specification {
  // ── (root) ──
  deploymentNode planetscale_my_sqlbranch {
    notation 'Planetscale.MySQLBranch'
    style {
      shape rectangle
    }
  }
  deploymentNode planetscale_my_sqldatabase {
    notation 'Planetscale.MySQLDatabase'
    style {
      shape rectangle
    }
  }
  deploymentNode planetscale_my_sqlpassword {
    notation 'Planetscale.MySQLPassword'
    style {
      shape rectangle
    }
  }
  deploymentNode planetscale_postgres_branch {
    notation 'Planetscale.PostgresBranch'
    style {
      shape rectangle
    }
  }
  deploymentNode planetscale_postgres_database {
    notation 'Planetscale.PostgresDatabase'
    style {
      shape rectangle
    }
  }
  deploymentNode planetscale_postgres_default_role {
    notation 'Planetscale.PostgresDefaultRole'
    style {
      shape rectangle
    }
  }
  deploymentNode planetscale_postgres_role {
    notation 'Planetscale.PostgresRole'
    style {
      shape rectangle
    }
  }
}
